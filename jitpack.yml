before_install:
  - export JAVA_HOME=/usr/lib/jvm/java-17-openjdk
  - export PATH=$JAVA_HOME/bin:$PATH
  - /usr/bin/apt-get update
  - /usr/bin/apt-get install maven -y
install:
  - mvn install -DskipTests=true